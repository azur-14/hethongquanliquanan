import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFF7B2C);       // cam chủ đạo
  static const Color primaryDark = Color(0xFFE36F29);   // cam nhấn (hover/pressed)
  static const Color dark = Color(0xFF2C2C45);          // nền tối
  static const Color background = Color(0xFFFFF2EA);    // nền sáng
  static const Color border = Color(0xFFE0E0E0);        // viền nhẹ
  static const Color text = Color(0xFF2C2C45);          // chữ chính
  static const Color textSecondary = Color(0xFF888888); // chữ phụ
}
